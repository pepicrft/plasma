{
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cargo run -p app -- serve\" \"pnpm --dir frontend dev\"",
    "build": "pnpm --dir frontend build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.4.0",
    "lucide-react": "^0.562.0"
  }
}
